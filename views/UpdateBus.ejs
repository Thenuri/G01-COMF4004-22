<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TESTING</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="/css/commonstyles.css">
    <link rel="stylesheet" href="/css/syntax2.css">
    
</head>
<body style="height:100vh; overflow: hidden;">
    <div class="header1">
        <header>
            <!--nav bar-->
            <nav class="navbar fixed-top navbar-expand-lg navbar-light p-md-3">
                <div class="container">
                    <!-- <a href="#" class="navbar-brand text-primary fs-1" style="margin-left: -70px;">Book My Bus</a> -->
                    <button type="button"class="navbar-toggler bottom-"data-bs-target="#navbarNav"data-bs-toggle="collapse"aria-controls="navbarNav"aria-expanded="false"aria-label="Toggle Navbar">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarNav">
                        <div class="mx-auto"></div>
                        <ul class="navbar-nav fs-5">
                            <li class="nav-item"><a href="/vehicle" class="nav-link text-dark">Book Now</a></li>
                            <li class="nav-item"><a href="#" class="nav-link text-dark">About Us</a></li>
                            <li class="nav-item"><a href="#" class="nav-link text-dark">Contact Us</a></li>

                            <!-- User name and pic here with a drop down -->
                            <li class="nav-item ">
                                
                                <div class="dropdown">
                                    <button class="btn shadow-none dropdown-toggle text-dark " type="button" id="userNameDropDown" data-bs-toggle="dropdown" aria-expanded="false">
                                        <img src="/images/profilepic1.jpg" class="profilePic" alt="">
                                      <span class="fs-5">Joe Biden</span>
                                    </button>
                                    <ul class="dropdown-menu" aria-labelledby="userDropDown">
                                      <li><a class="dropdown-item" href="/profile">My Profile</a></li>
                                      <li><a class="dropdown-item" href="/dashboard">Dashboard</a></li>
                                      <li><a class="dropdown-item" href="/signout">Sign out</a></li>
                                    </ul>
                                  </div>
                            </li>
                        </ul>
                    </div>
                </div>
            </nav>
            </div>
        </header>
    </div>

    <div class="dashboard-grid" style="height:100%;">

        <div class="sidebar" style="background-color: #240A6B;">
            <a href="#" class="navbar-brand text-white fw-bold fs-1 mx-5">Book My Bus</a><br>
            <div class="mx-5 mt-5 text-white  fs-3">Owner Dashboard</div>
            <ul class="list-group-horizontal list-unstyled mx-5 " id="dashboard-menu">
                <li><a class="text-white-50 text-decoration-none fs-5"  href="#upcoming-trips">Upcoming Trips</a></li>
                <li><a class="text-white-50 text-decoration-none fs-5" href="#pending-confirmation">Pending confirmation</a></li>
                <li><a class="text-white-50 text-decoration-none fs-5" href="#prev-trips">Previous Trips</a></li>
                <li><a class="text-white-50 text-decoration-none fs-5" href="#cancelled-trips">Cancelled Trips</a></li>
                <li><a class="text-white-50 text-decoration-none fs-5" href="#manage-buses">Manage buses</a></li>
                <li><a class="text-white-50 text-decoration-none fs-5"href="#" style="position:absolute; bottom:1rem ;">My Profile</a></li>
            </ul>
        </div>

        <!-- UPDATE BUS FORM -->
        <div class="container-fluid mx-auto">
            <div class="row d-flex flex-nowrap">
                
                    <div class="old col-lg-5">
                      <form>
                        <h1 class="Update">Update Bus Details</h1><br><br>
                        <div class="row mb-3">
                            <label for="inputBusNo" class="col-sm-3 col-form-label">Bus Number:</label>
                            <div class="col-sm-7">
                              <input type="text" class="form-control" id="BusNo">
                            </div>
                          </div>

                          <div class="row mb-3">
                            <label for="inputSeatNo" class="col-sm-3 col-form-label">Number of Seats:</label>
                            <div class="col-sm-7">
                              <input type="number" class="form-control" id="SeatNo" min="0">
                            </div>
                          </div>

                          <div class="row mb-3">
                            <label for="inputBusNo" class="col-sm-3 col-form-label">Price per/km:</label>
                            <div class="col-sm-7">
                              <input type="text" class="form-control" id="priceKM">
                            </div>
                          </div>

                          <div class="row mb-3">
                            <label for="inputBusNo" class="col-sm-3 col-form-label">Driver Name:</label>
                            <div class="col-sm-7">
                              <input type="text" class="form-control " id="DName">
                            </div>
                          </div>

                          <div class="row mb-1">
                            <label for="inputBusNo" class="col-sm-4 col-form-label">AC/ NON AC:</label>
                            <div class="col-sm-2">
                                <input class="form-check-input" type="radio" name="flexRadioDefault" id="AC">
                                <label class="form-check-label" for="flexRadioDefault1">
                                  AC
                                </label>
                              </div>

                              <div class="col-sm-4">
                                <input class="form-check-input" type="radio" name="flexRadioDefault" id="NONAC">
                                <label class="form-check-label" for="flexRadioDefault1">
                                  NON AC
                                </label>
                              </div>
                          </div><br><br>

                          <div class="lol">
                            <div class="hi mb-3 px-1">
                                <center><button type="submit" class="btn btn-blue text-center" id="delete">Delete Bus</button></center>
                            </div>
            
                            <div class="hi mb-3 px-1">
                                <center><button type="submit" class="btn btn-blue text-center" id="update">Update Bus</button></center>
                            </div>
                        </div>
                    
                     
                          </form>
                    </div>
                    <!-- Bus Upload Image -->
                    <div class="sam col-lg-5">
                      <div class="col-6 ">
                        <div id="bus-img-display"></div>
                        <input type="file" name="bus-image" id="bus-img-upload">
                      </div>
                    </div>

                    
                </div>
            </div>
        </div>    
        <footer>
          <p class="copyright"><br><br><br><br><br><br><br>
               &copy;  2022 Book My Bus. 
          </p>
      </footer>

        
    </div>

    <script>

        // Display uploaded image
        // select the elements
        const imageUploadInput = document.getElementById("bus-img-upload")
        const imageDisplay = document.getElementById("bus-img-display")

        imageUploadInput.addEventListener( "change", displayImg )

        function displayImg() {
            console.log("Upload")
            imgObjUrl = URL.createObjectURL(imageUploadInput.files[0])
            imageDisplay.style.backgroundImage = `url(${imgObjUrl})`;
        }

        // Sidebar menu link color to white for selected link.
            // get a node list of the sidebar links
            const sidebarMenuLinks = document.querySelectorAll(".sidebar li a");

            sidebarMenuLinks.forEach( (link) => {
                link.addEventListener("click", sidebarMenuColorChange);
            });

            // The colors of the sidebar menu
            const colorActiveBsClass = "text-white";
            const colorDefaultBsClass = "text-white-50";

            function sidebarMenuColorChange(e) {
                link = e.target;

                // switch the color of the link to the active color when clicked
                link.classList.replace(colorDefaultBsClass, colorActiveBsClass);

                // switch colors of the other links to default color
                sidebarMenuLinks.forEach(element => {
                    if (element !== link) {
                        element.classList.replace(colorActiveBsClass, colorDefaultBsClass);
                    }
                });
            }


    </script>
    <script>
      document.getElementById("update").addEventListener("click",update);
      function update(event){
        event.preventDefault();

        let BusNo = document.getElementById("BusNo").value;
        let SeatNo = document.getElementById("SeatNo").value;
        let priceKM = document.getElementById("priceKM").value;
        let DName = document.getElementById("DName").value;
        // let AC = document.getElementById("AC").value;
        // let NONAC = document.getElementById("NONAC").value;

        let BusValues ={
          Bus_No: BusNo,
          No_Of_Seats: SeatNo,
          Price_Per_km: priceKM,
          Driver_Name: DName
        }
        axios.patch('/ManageBus/BusUpdate',BusValues)
        .then((res)=>{
          console.log("Successfull")
        })
      }

      document.addEventListener("DOMContentLoaded",getvalues)
      function getvalues(){
        const urlPath = "ManageBus/BusFill"
      
      // For redirect param
      const params = new URLSearchParams(window.location.search);
            const redirectURL = params.get("redirect")
            let url;
            if (redirectURL) {
                url =`${urlPath}?redirect=${redirectURL}`;
            } else {
                url = `${urlPath}`;
            }
        
            axios.get(url)
            .then( (res) => {

                axiosReqUrl = window.location.origin + res.config.url  // url axios sent the request to

                //  if the url axios sent the request and the respose url from the server doesnt match a redirection has occured
                if(axiosReqUrl !== res.request.responseURL) {
                    window.location.href = res.request.responseURL;  // set the window location to redirect

                    
                }

                let BusNo = document.getElementById("BusNo");
                let SeatNo = document.getElementById("SeatNo");
                let priceKM = document.getElementById("priceKM");
                let DName = document.getElementById("DName");

                BusNo.value = BusDetails.Bus_No;
                SeatNo.value = BusDetails.Address;
                priceKM.value = BusDetails.Contact_No
                DName.value = BusDetails.Contact_No

            })
            .catch(e=> {
                console.log(e)
                alert(e.response.data.error.message)
            })
      }
    </script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js" integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js" integrity="sha384-cVKIPhGWiC2Al4u+LWgxfKTRIcfu0JTxR+EQDz/bgldoEyl4H0zUF0QKbrJ0EcQF" crossorigin="anonymous"></script>
</body>
</html>